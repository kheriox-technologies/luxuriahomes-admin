/**
 * Folder that holds all documents uploaded by clients through the portal.
 * `NAME` is the display name shown in the admin file manager; `PATH` is the
 * kebab slug used as both the folder `path` and the document `folderPath`
 * (mirrors how `projectDocuments/createFolder` slugifies folder names).
 */
export const CLIENT_UPLOADS_FOLDER_NAME = 'Client Uploads';
export const CLIENT_UPLOADS_FOLDER_PATH = 'client-uploads';
